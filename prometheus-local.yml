global:
  scrape_interval: 5s # Set to 5s for fast updates during debugging (Course says 15s normally)

scrape_configs:
  - job_name: 'dispatcher-service'
    static_configs:
      #- targets: ['127.0.0.1:8001']
      - targets: ['host.docker.internal:9000']